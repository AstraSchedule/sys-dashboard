{
  "name": "astra-dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "docker-dev": "bun install && vite --host",
    "docker-preview": "bun install && vite build && vite preview"
  },
  "dependencies": {
    "@vicons/ionicons5": "^0.13.0",
    "axios": "^1.7.0",
    "vue": "^3.5.0",
    "vue-request": "^2.0.4",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "naive-ui": "^2.40.0",
    "vite": "^6.0.0"
  },
  "overrides": {
    "postcss": "^8.5.18"
  }
}
